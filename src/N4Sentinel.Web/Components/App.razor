@using N4Sentinel.Web.Securite
@inject IHttpContextAccessor Accesseur
<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <ResourcePreloader />
    <link rel="stylesheet" href="@Assets["app.css"]" />
    <link rel="stylesheet" href="@Assets["N4Sentinel.Web.styles.css"]" />
    @* La carte d'importation est un script en ligne : elle doit porter le jeton de la
       requête, sinon la politique de contenu la bloque et les modules ne se chargent plus. *@
    <ImportMap nonce="@Jeton" />
    <HeadOutlet />
</head>

@* Le thème est appliqué côté serveur, au rendu : la page arrive déjà dans la bonne
   couleur, sans le clignotement d'un basculement fait après coup en JavaScript. *@
<body class="@ClasseDeTheme">
    <Routes />
    <ReconnectModal />
    <script src="@Assets["_framework/blazor.web.js"]"></script>
    <script src="@Assets["js/interface.js"]"></script>
</body>

</html>

@code {
    /// <summary>
    /// Thème retenu, lu dans un cookie. Le sombre est le défaut — c'est celui de la maquette
    /// validée, et celui d'un écran de supervision consulté en salle technique.
    /// </summary>
    private string? ClasseDeTheme =>
        Accesseur.HttpContext?.Request.Cookies["theme"] == "clair" ? "theme-clair" : null;

    /// <summary>Jeton de la politique de contenu, tiré par requête.</summary>
    private string? Jeton => Accesseur.HttpContext.JetonDeScript();
}
